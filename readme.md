# Webpack Typescript React Skeleton

## Commands

Install dependencies:

```sh
yarn install
```

Build app:

```sh
yarn build
```

Serve app in development mode:

```sh
yarn dev
```

Clean project:
```sh
yarn clean
```

## Running app after build

```sh
yarn start
```